{
  "name": "os-tools",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"backend\\.venv\\Scripts\\python -m uvicorn app.main:app --reload --app-dir backend\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
